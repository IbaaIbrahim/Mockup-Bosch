import { OpsConsoleClient } from './OpsConsoleClient';

export const metadata = { title: 'Ops Console — Bosch Parcel Platform' };

export default function OpsPage() {
  return <OpsConsoleClient />;
}
